session_name: servers
windows:
  - window_name: servers
    layout: even-horizontal
    shell_command_before:
      - exec /bin/bash # make sure we're using bash
    panes:
      - shell_command: # backend server
          - source ./backend/8900-env/bin/activate
          - just host-backend
      - shell_command: # frontend server
          - just host-frontend
